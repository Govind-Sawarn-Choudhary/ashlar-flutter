abstract final class AppDurations {
  static const Duration splashDisplay = Duration(milliseconds: 2500);
  static const Duration splashFadeIn = Duration(milliseconds: 900);
  static const Duration splashScaleIn = Duration(milliseconds: 1100);
}
